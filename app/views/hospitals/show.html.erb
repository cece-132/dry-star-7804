<h1> <%= @hospital.name %> </h1>

<div class="doctors">
  <% @hospital.doctors.desc_pt_count.each do |dr| %>
    <div id="doctor-<%=dr.id%>">
      <%= dr.name %> Patient Count: <%= dr.patient_count %> <br>
    </div>
  <% end %>
</div>